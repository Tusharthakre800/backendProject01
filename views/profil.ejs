<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <link rel="stylesheet" href="../style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
             <a class="bg-red-500 rounded-md px-3 py-2 text-sm md-5 inline-block " href="/logout"> logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10  rounded-md">
              <a href="/profile/upload"><img class="w-full h-full object-cover rounded-full" src="/images/upload/<%= user.profilepic %>" alt=""></a>  
            </div>
        <h1 class="text-3xl"><span class="font-light">hello</span> , <%= user.name %></h1>
        </div>
        <h5 class="mb-5 mt-3">you can create a new post</h5>
        <form action="/post" method="post">
            <textarea required placeholder="what's on your mind ?" class=" block w-1/3 resize-none  bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
            <input type="submit" class="px-3 w-40 py-2 bg-blue-500 block rounded-md  mt-2" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">your Posts.</h3>
            <div class="postconiter mt-5" >
                <% user.post.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 mb-4 bg-zinc-800 rounded-md border-[1px] border-zinc-800">
                        <h4 class=" text-blue-500  mb-2"><%= user.username%></h4>
                        <p class="text-sm tracking-tight"><%= post.content%></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                        <div class="btns flex gap-4 mt-1"></div>
                            <a class="cursor-pointer text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike "%>
                            </a>
                            <a class="text-zinc-400 cursor-pointer" href="/edit/<%= post._id %>">edit</a>
                            <a class="text-zinc-400 cursor-pointer" href="/erase/<%= post._id %>">erase</a>
                        </div>
                    </div>
               <% } )  %>
              
                
            </div>
        </div>
       
    </div>
</body>
</html>